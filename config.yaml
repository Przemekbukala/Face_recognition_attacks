attacks:
  - name: "bitflip"
    parameters:
      bits_to_flip: 50

  - name: "grid_occlusion"
    parameters:
      rho_grids: 8
      line_width: 1
  - name: "grid_occlusion"
    parameters:
      rho_grids: 16
      line_width: 1